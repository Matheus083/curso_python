# Metodos em instancias de classes Python.

class Car:
    # def __init__(self, something='Null'):
    #     self.name = something

    # def run(self):
    #     print(f"{self.name} is run....")
    ...

fusca = Car()
fusca.name = 'Fusca'
print(fusca.name)
# fusca.run()
