amount_of_lines = 5
amount_of_columns = 5
lines = 1 

while lines <= amount_of_lines:

    columns = 1

    while columns <= amount_of_columns:
        
        print(f'Line {lines} - Column {columns}')
        columns += 1
    lines += 1

print('Finished!')
